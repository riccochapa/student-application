extends ../layout

block content
  header
  nav.navbar.navbar-fixed-top
    .container-fluid
      .navbar-header
        button.navbar-toggle.collapsed(type='button' data-toggle='collapse' data-target='#myNavbar' aria-expanded='false')
          span.sr-only Toggle Navigation
          span.icon-bar
          span.icon-bar
          span.icon-bar
        a.navbar-brand(href='/home') (boot)(up)
      #myNavbar.collapse.navbar-collapse
        ul.nav.navbar-nav.navbar-right
          li.active
            a(href='/admin') HOME
            span.sr-only(current)
          li.active
            a(href='/admin/accepted') ACCEPTED
            span.sr-only(current) 
          li.active
            a(href='/admin/waitlisted') WAITLIST
            span.sr-only(current)
          li.active
            a(href='/admin/alternative') ALTERNATIVE
            span.sr-only(current) 
          li.active
            a(href='/logout') LOGOUT
            span.sr-only(current)
            
  br
  div#belownav
  div
  br
  h1.mini waitlisted applicants
  br
  div#applicantreview
  br
  br
  br
  div
    each stat in profile
      - var appStat = stat.application.status
      if appStat == "waitlist"
        dl
          button.list-group-item
              dt #{stat.name.firstName} #{stat.name.lastName}
              br
              a.btn.btn-xs.btn-default#adminsubmit(href='/admin/review/#{stat.id}') REVIEW
            br
