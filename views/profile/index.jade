extends ../layout

block content
  head
    nav.navbar.navbar-fixed-top
      .container
      .navbar-header
      button.navbar-toggle(type='button', data-toggle='collapse', data-target='#myNavbar')
        span.icon-bar
        span.icon-bar
        span.icon-bar
      a.navbar-brand(href='/admin') (boot)(up)
        #myNavbar.collapse.navbar-collapse
      ul.nav.navbar-nav.navbar-right
        li
          a(href='/home') HOME
        li
          a(href='/profile') PROFILE  
        li
          a(href='/application') APPLICATION
        li
          a(href='/application/status') STATUS   
        li
          a(href='/logout') LOGOUT

    div#profile.container.slide
      br
      br
      h1.easy profile
      br
      br
      br
      .panel.panel-default.slide
        .panel-heading
          h3.panel-title #{profile.name.firstName} #{profile.name.lastName}
        .panel-heading
          h5.panel-title #{profile.name.preferredName} 
      br
      .panel.panel-default.slide#questionaire 
        .panel-heading
          h3.panel-title contact:
        .panel-body
          h5.panel-title #{profile.contact.address}
          h5.panel-title #{profile.contact.email}
          h5.panel-title #{profile.contact.phone}
      br
      .panel.panel-default.slide#questionaire 
        .panel-heading
          h3.panel-title social:
        .panel-body
          h5.panel-title #{profile.social.github}
          h5.panel-title #{profile.social.facebook}
          h5.panel-title #{profile.social.twitter}
      br
      br     
      a.btn.btn-default#profilebutton.easy(href='/profile/edit') edit profile
      br     
